CREATE TABLE accession_numbers (accession_number TEXT, object_id TEXT, organization_id INTEGER);
CREATE UNIQUE INDEX by_accession_number on accession_numbers (accession_number, object_id, organization_id);

CREATE TABLE organizations (organization_uri TEXT, organization_id INTEGER);
CREATE UNIQUE INDEX by_organization on organizations (organization_uri);